# 横沟采购清单及产品说明模板规律

当用户要求“按模板”“按横沟模板”“按照上述文件夹一样”整理时，优先按本规律学习格式、颗粒度和归类方法；如用户提供了新的模板文件夹，应先读取新模板并以新模板为准。

重要：本文件中的分类名称是横沟案例的参考形态，不是固定枚举，也不是分类依据。**分类完全由当前工程实际物料和 SKILL.md「动态分类方法论」决定**；本文件只负责提供输出结构与格式参考（Excel 行结构、续写行、产品说明规律）。

## 模板路径

模板文件夹由用户提供路径；技能包内如有 `template/` 目录可优先使用。无模板时按本文件的分类规律整理。

## 目录结构

模板不是一个总表，而是分类文件夹结构。以下是横沟项目中的示例分类：

- `PLC 模块 + 仪表传感器/`
- `光纤网线类/`
- `安装辅材杂项/`
- `安防车辆管控/`
- `工控服务器 UPS/`
- `显示大屏看板/`
- `机柜箱体配电/`
- `线缆管材类/`
- `网络交换设备/`
- `软件定制服务/`
- 根目录 `未分类项目.xlsx`

输出时必须建立分类子文件夹，但不要求完全等同于以下分类。不能用一个总 Excel 或一个总 Word 替代分类文件夹。分类名称和数量应由当前清单决定。

## Excel 格式规律

分类 Excel 通常为：

- 第 1 行：分类标题，如 `线缆管材类`。
- 第 2 行：表头。
- 第 3 行开始：数据。
- 常见表头：`序号 / 设备名称 / 技术参数 / 数量 / 单位`。
- 部分分类使用变体表头：
  - `安装辅材杂项`：`序号 / 器材名称 / 技术参数 / 数量 / 单位`
  - `软件定制服务`：`序号 / 软件名称 / 技术特征 / 数量 / 单位`
- 部分 Excel 有第 6 列空列，应保持不影响主 5 列整理。

### 列名改写规则

从原始清册整理到采购清单时，列名按以下映射改写：

- `顺序` → `序号`
- `设备材料名称` → `设备名称`
- `产品目录中的型号及规格` → `技术参数`
- `单位`、`数量` → 调整为 `数量` 在前、`单位` 在后

### 数量与参数整理口径

- 数量是按采购类别重新统计的汇总数，不是按原始章节顺序逐行搬运。
- 技术参数允许把原始长句规范化为适合采购询价的表达，但核心型号、规格、数量、单位必须保留；不得虚构原文没有的参数。

同一设备有多个规格时，模板允许后续行 `序号`、`设备名称` 留空，仅填写 `技术参数 / 数量 / 单位`。例如：

- `光纤` 首行写设备名称和第一种参数；
- 下一行设备名称留空，只写 `单模8芯铠装 / 4300 / M`。

这种行表示同一设备的不同规格。提取时按「断行和续写行」的自动补全规则补全设备名称并标注来源；生成分类 Excel 时可按模板格式呈现为空名称续写（首行写名称、后续行留空），但底层数据必须能追溯到同一设备名称。

## 分类规则

本文件的分类内容属于**横沟案例**的参考形态（完整案例见 `references/category_cases.md` 案例 1）。

分类遵循「动态分类方法论」（见 SKILL.md 对应章节），三条核心原则：

1. **边界原则**：每类只放符合该类设备类型的设备；硬塞到近似但不匹配的分类属于违规。
2. **粒度原则**：默认按案例大类归档；只有某类条目特别多、确需拆分时才按设备类型拆细，且不能拆成只有 1~2 项的碎片类。
3. **动态原则**：案例分类只是参考；当前清单出现案例没有覆盖的设备类型应新建分类，拿不准的放入 `未分类项目.xlsx`。

分类判断优先级：**工程功能 > 所属子系统 > 设备/材料属性 > 安装与采购关联性**，禁止只根据关键词分类（不能因名称含"交换"就归网络设备、含"柜"就归机柜类）。必须结合设备是什么、在当前工程里做什么、属于什么系统、与哪些设备共同工作、该专业规模综合判断。

分类必须**先整体分析再逐项分类**：先读取全部物料、识别工程类型与专业构成、读取最相似案例（复合工程读取多个）、形成候选骨架，再逐项映射并动态合并拆分，最后做类名自洽、粒度、唯一归类、未分类复核。不得读取一条立即定分类。写文件前必须先完成内部预分类，防止返工。

### 命名格式

统一采用两种格式，按主体类型选择：

- `XX类`：主体是材料/线缆/辅材。如 `光纤网线类`、`线缆管材类`、`安装辅材杂项`。
- `XX及XX设备/材料`：主体是设备/柜体，或一类含两种类型。如 `高低压配电柜及变压设备`、`防爆控制箱及现场电器`。

命名铁律：文件夹名必须一眼看出里面装什么，禁止用笼统大类名装内容不符的设备。

### 横沟案例参考分类（不是固定目录）

以下分类是横沟（选煤厂智能化）案例的参考形态，仅当当前工程与案例高度相似时参考；必须按当前清单动态删除不存在的类别、合并项目很少且关系紧密的类别、拆分项目很多且专业差异明显的类别、新增当前工程独有的类别：

| 分类名 | 装什么 |
| --- | --- |
| PLC 模块 + 仪表传感器 | PLC 模块、流量计、传感器、变送器、阀门 |
| 光纤网线类 | 光纤、光缆、网线、光纤收发器 |
| 安装辅材杂项 | 辅材、安装材料、支架、杂项 |
| 安防车辆管控 | 摄像机、门禁、车辆管控、人员定位 |
| 工控服务器 UPS | 工控机、服务器、UPS、工作站 |
| 显示大屏看板 | LED 屏、看板、显示器 |
| 机柜箱体配电 | 配电箱、机柜、控制柜、采集箱 |
| 线缆管材类 | 电缆、钢管、桥架、母线 |
| 网络交换设备 | 交换机、网关、防火墙 |
| 软件定制服务 | 软件、平台 |

供电类项目（案例未覆盖时）可新建清晰分类：`高低压配电柜及变压设备`、`保护自动化及直流设备`、`防爆控制箱及现场电器`、`配电室暖通设备`、`照明及现场开关设备`（见 `references/category_cases.md` 案例 2）。

注意：如果当前工程与横沟案例高度相似，却最终生成「电子设备类」「综合设备类」「电气设备类」「其他材料类」这类明显过粗、信息量低且偏离成熟案例的分类，应重新检查。反之，供配电、暖通等其他工程不得硬套弱电分类。

## 分类辅助线索（横沟案例内）

以下关键词只作为**横沟案例内的辅助验证线索**，**不是分类依据，更不是固定白名单**。

禁止只根据关键词分类。关键词只能用于：整体分析后对候选归类做快速复查；不能因为名称含"柜""交换""箱"等字就决定分类。分类判断必须按「动态分类方法论」结合工程功能、所属子系统、来源章节、规格参数、单位与上下文综合确定。

> 说明：随着案例库（`references/category_cases.md`）积累，新的工程分类知识优先整理进案例库，不再向本文件追加设备关键词规则。

### PLC 模块 + 仪表传感器（案例参考）

适合：PLC 模块、I/O 模块、通信模块、模拟量/数字量模块、流量计、浓度计、浊度计、界面仪、阀门、传感器、变送器、拉绳传感器、多工况数据处理器。

辅助词：`PLC`、`模块`、`模拟量`、`数字量`、`RS485`、`以太网通讯`、`流量计`、`浓度计`、`浊度计`、`界面仪`、`传感器`、`变送器`、`电控阀`、`拉绳`。

### 光纤网线类

适合：光纤、光缆、网线、光纤跳线、光纤收发器等网络传输介质。

辅助词：`光纤`、`光缆`、`跳线`、`网线`、`光纤收发器`、`单模`、`多模`。

### 安装辅材杂项

适合：辅材、安装材料、安装套件、立杆、支架、密封罩、鼠标键盘、零星成套材料、难以归入其他类但明显属于安装配套的材料。

辅助词：`辅材`、`安装材料`、`安装套件`、`支架`、`立杆`、`密封罩`、`扎带`、`绝缘胶布`、`角铁`、`槽钢`、`管件`、`杂项`。

### 安防车辆管控

适合：摄像机、枪机、门禁、道闸、雷达、声光报警器、红外探测、车辆识别、定位、巡检机器人、环境安防设备。

辅助词：`摄像机`、`枪机`、`门禁`、`道闸`、`雷达`、`车牌`、`车辆`、`声光报警`、`红外`、`探测器`、`磁力锁`、`巡检机器人`、`定位`、`安防`。

### 工控服务器 UPS

适合：服务器、工控机、操作工作站、笔记本、Pad、硬盘、超融合、视频分析服务器、UPS、授时服务器。

辅助词：`服务器`、`工控机`、`工作站`、`笔记本`、`Pad`、`硬盘`、`超融合`、`UPS`、`授时`。

注意（自洽护栏）：本类是横沟案例中的「计算存储类」参考形态。若当前工程生成同名分类，应做类名自洽校验（见 SKILL.md「动态分类方法论」第 9 条）：类内设备必须能被「工控服务器 UPS」这个类名合理解释（服务器、工控机、UPS、存储等计算存储类设备）。路灯、风机、空调、电缆、开关柜、液位计等无法被该类名解释的设备不得混入；它们应按其工程功能归入其他合适的分类。本护栏仅在该分类真实存在时生效，不要求当前工程必须存在或新建该分类。

### 显示大屏看板

适合：LED 屏、液晶看板、显示器、生产看板、任务看板、报警看板、仓存看板、环境监测主机等显示或看板类设备。

辅助词：`LED`、`显示屏`、`大屏`、`看板`、`显示器`、`液晶`。

### 机柜箱体配电

适合：机柜、控制柜、配电箱、采集箱、I/O 机架、电源模块、智能照明灯具、模块化机房、现场箱体。

辅助词：`机柜`、`柜`、`箱`、`配电`、`控制柜`、`采集箱`、`I/O机架`、`电源模块`、`模块化机房`、`LED灯具`。

注意（自洽护栏）：本类是横沟案例中的「机柜/箱体/机架/模块」参考形态。若当前工程生成同名分类，应做类名自洽校验（见 SKILL.md「动态分类方法论」第 9 条）：类内设备必须能被「机柜箱体配电」这个类名合理解释（机柜、箱体、机架、模块类）。高压开关柜、干式变压器、低压进线柜、电容补偿柜、变频器等配电设备本体无法被该类名解释，应按其工程功能归入供配电相关分类（如「高低压配电柜及变压设备」——是否创建该分类、叫什么，由当前工程物料决定）。防爆控制箱、现场电器、液位计、暖通风机空调、保护直流屏等同理按工程功能归类。本护栏仅在该分类真实存在时生效。

### 线缆管材类

适合：各类电缆、电源线、控制电缆、信号线、双绞线、钢管、金属槽盒、桥架、VGA 延长线等。

辅助词：`电缆`、`电源线`、`线缆`、`控制线`、`信号线`、`双绞线`、`钢管`、`槽盒`、`桥架`、`管材`、`延长线`、`母线桥`、`接地线`。

### 网络交换设备

适合：核心交换机、汇聚交换机、工业交换机、接入交换机、监控交换机、智能网关等网络设备。

辅助词：`交换机`、`核心交换`、`汇聚交换`、`接入交换`、`工业级交换`、`网关`。

### 软件定制服务

适合：软件、平台、数据采集、WEB 发布、组态、算法、信息安全软硬件、工业数据中台、专家知识库、系统定制服务。

辅助词：`软件`、`平台`、`WEB`、`组态`、`算法`、`数据采集`、`中台`、`知识库`、`信息安全`、`系统`、`服务`。

### 易混装设备归类对照（供电类项目案例参考）

当前清单以电力专业为主时，参考榆树湾供电案例的分类形态（见 `references/category_cases.md` 案例 2）。以下只是**该案例的归类形态**，是否创建这些分类、叫什么、设备归入哪类，由当前工程实际物料和「动态分类方法论」决定，不得视为固定规则：

- 高压开关柜、干式变压器、电阻柜、低压进线/联络/馈出柜、电容补偿柜、有源滤波柜、软启动器柜、变频器柜 → 参考形态 `高低压配电柜及变压设备`
- 综合自动化保护装置、直流屏、自动化后台 → 参考形态 `保护自动化及直流设备`
- 防爆风机箱、防爆检修电源箱、防爆刀开关、液位计、浮球开关、现场按钮盒 → 参考形态 `防爆控制箱及现场电器`
- 轴流风机、电暖风机、空调 → 参考形态 `配电室暖通设备`
- 路灯、路灯控制箱、负荷开关 → 参考形态 `照明及现场开关设备`
- 高压/低压电缆、桥架、封闭母线桥、接地线 → 参考形态 `电缆桥架及接地材料`
- 服务器、工控机、操作工作站、笔记本、存储硬盘、超融合、视频分析服务器、UPS、授时服务器 → 参考形态 `工控服务器 UPS`

关键判断：路灯、轴流风机、空调等设备按其**工程功能**归入合适的分类（照明、暖通等），不得因为类名含"服务器/UPS"就混入计算存储类——这是「类名自洽」护栏的要求（见 SKILL.md「动态分类方法论」第 9 条），而非要求必须创建某个固定分类。

### 安装工序行剔除（强规则）

名称以“安装/敷设”结尾、来自安装工程费项的工序行（如 `电缆敷设`、`立杆安装`、`照明灯具安装`、`水泵安装`、`水泵配管`、`桥架安装`、`钢管敷设`）**不是设备材料**，一律剔除，不进原始清单和任何采购分类，也不进入「未分类项目.xlsx」。

示例参考：榆树湾供电系统采购清单及产品说明（6 类清晰分类）。

## 未分类项目

无法可靠归类时，放入根目录 `未分类项目.xlsx`，不要硬塞分类。

`未分类项目.xlsx` 模板较简单，通常只有 4 列数据：名称、参数、单位、数量。整理时可保持该结构；如用户要求统一，也可增加表头，但需说明。

## 产品说明 Word 规律

每个分类子文件夹内通常有一个产品说明 Word。规律：

- 一级标题通常为 `{分类名称}产品说明` 或同义标题。
- 正文按 `一、二、三...` 分节，必要时含三级标题。
- 内容不是短介绍，而是工程化说明，通常包含：
  - 系统或场景适用范围；
  - 主要设备用途；
  - 配套关系；
  - 施工、安装、接入或运维要求；
  - 采购核验重点。
- 对原文没有的型号、品牌、性能指标不得编造。
- 空白规格设备只能说明用途和核验要求，不能写确定型号。

生成产品说明时，优先参考同类模板 Word 的标题密度和语气。例如：

- 线缆、桥架、电缆类参考 `线缆管材类/线管、线缆、管材桥架类产品说明.docx`。
- 柜体、箱体、配电箱类参考 `机柜箱体配电/机柜箱体配电产品说明.docx`。
- 交换机、网关类参考 `网络交换设备/网络交换设备产品说明.docx`。
- 服务器、UPS 类参考 `工控服务器 UPS/工控服务器、UPS 产品说明.docx`。
- 摄像机、门禁、车辆识别、报警类参考 `安防车辆管控/安防车辆管控类产品说明.docx`。

如果当前清单的新分类没有完全对应的模板 Word，应选择最接近的模板学习写法，再按新分类名称和设备内容重写。例如供配电柜类可参考 `机柜箱体配电` 的说明结构，但标题和正文必须改成供配电设备自身内容。

## 输出核查

完成后必须核查：

- 是否生成了分类子文件夹，而不是单一总表。
- 分类是否根据当前清单动态确定，未机械套用不匹配的横沟分类。
- 每个有数据分类是否同时有 Excel 和 Word。
- 分类 Excel 第 1 行是否为分类标题，第 2 行是否为表头。
- 参数不同的同名设备是否没有被合并。
- 续写行是否保持参数独立，未错误合并数量。
- 空白规格是否进入 `未明确确认部件规格参数.xlsx`。
- 无法归类项是否进入 `未分类项目.xlsx`。
