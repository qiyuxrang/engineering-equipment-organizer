---
name: engineering-equipment-organizer
description: 当需要从工程项目的 Word、PDF、Excel、WPS 或资料文件夹中整理设备材料清单时使用。本技能用于生成原始设备材料清单、按模板规律整理采购清单及产品说明文件夹、筛出规格参数空白项、检索可补充候选参数，并给出严格核查汇报。适用于矿井、选煤厂、高速公路、市政、机电、电气、智能化等工程资料。
---

# 工程设备材料清单整理

## 用途

用于工程资料中的设备、材料、器材清单整理工作，重点是准确性、可追溯和避免误合并。完整流程包括：

- 从 Word、PDF、Excel、WPS 等原始资料中提取设备材料行。
- 生成原始设备材料清单 Excel。
- 按「动态分类方法论」（案例定形态、方法论定调整、护栏定边界、当前工程定最终结果）生成 `采购清单及产品说明/`。
- 按采购口径合并重复项，但不合并不同规格参数。
- 将规格参数为空的设备单独整理为「未明确确认部件规格参数」Excel。
- 在项目资料文件夹中检索可补充的候选参数，但不直接覆盖不确定数据。
- 最终汇报输出文件、数量统计、核查结果和需要人工确认的问题。

## 强制规则

- 始终用简体中文向用户汇报。
- 严禁修改原始 Word、PDF、Excel、WPS 文件；只能读取，必要时转换临时副本。
- 输出文件统一放到**桌面**（`~/Desktop/`），并命名清楚（如 `{项目名}整理输出/` 或 `{项目名}采购清单及产品说明/`）；用户明确指定目录时按用户指定执行。**不得随意放置**到源文件目录、临时目录或项目资料深处。
- 不猜测规格、型号、数量、单位。
- 严禁随意更改设备名称：设备名称必须忠实于原文，不得为归类、合并或排版而改写、缩写、换词。
- 不静默合并规格不同的设备。
- 不把候选参数直接回填到主采购清单，除非用户明确要求且来源完全明确。
- 不确定内容必须保留并标注为需要人工确认。

## 默认输出

除非用户另有要求，默认生成：

1. `原始设备材料清单.xlsx`
2. `采购清单及产品说明/`（按「动态分类方法论」拆成分类子文件夹，不得只生成一个总表）
3. `未明确确认部件规格参数.xlsx`

默认不单独生成严格核查报告文件，只在最终回复中汇报核查结果。只有用户明确要求时，才另存核查报告文件。

## 配套资源

本技能包含可复用资源，执行复杂任务时优先使用：

- `scripts/extract_project_text.py`：把项目资料文件夹中的 Word、WPS、PDF、Excel 转成可检索文本库。
- `scripts/scan_blank_specs.py`：从 `采购清单及产品说明/` 中扫描规格参数为空的设备，生成 `未明确确认部件规格参数.xlsx`。
- `scripts/check_procurement_consistency.py`：检查采购 Excel 与产品说明 Word 的条目数、设备名称、标题编号、异常标点、说明书依据关键词和 DOCX 渲染状态。
- `references/category_cases.md`：工程分类案例库（选煤厂/横沟、供配电/榆树湾、智能化弱电、暖通动力等案例）。**分类前必须读取**，按案例学习分类颗粒度和命名，但案例不是固定白名单，最终分类由当前工程实际物料决定。
- `references/henggou_template_pattern.md`：横沟 `采购清单及产品说明` 模板的目录结构、Excel 格式、续写行和产品说明规律。当用户要求按横沟模板或默认模板整理时，必须先读取，但不能把模板分类名称机械套用到所有项目。
- `references/product_description_template.md`：产品说明 Word 的默认章节骨架。生成或优化产品说明 Word 时，如果用户没有提供更强模板，必须先读取该参考文件。

## 输出与备份规范

- 修改既有文件夹前，先备份到 `outputs/<任务名>/backup_before_.../`。
- 临时转换文件放到 `outputs/<任务名>/converted/` 或 `outputs/<任务名>/text/`。
- **最终交付目录统一放桌面**（`~/Desktop/`），命名带项目名（如 `{项目名}整理输出/`）；`outputs/` 只用于备份和临时文件，不作为交付位置。
- 校验结果口头向用户汇报即可，**不在交付文件夹内生成 .json 或其他校验报告文件**，保持交付目录干净（如需留存可输出到系统临时目录 `/tmp/` 或 `outputs/<任务名>/_check/`，不得混入交付目录）。
- 不把临时文本、渲染图、脚本中间产物、校验文件混入用户交付目录。
- 最终交付为普通文件夹和文件结构，不打压缩包（zip/rar/7z 等），不生成压缩文件。
- 原始资料和用户提供的源文件始终只读。

## 标准列名

原始清单默认列名：

`顺序` / `设备材料名称` / `产品目录中的型号及规格` / `单位` / `数量`

如果用户或项目模板指定了其他列名，则按用户或模板要求调整。

采购清单及产品说明文件夹中的采购表，优先沿用模板格式，通常为：

`序号` / `设备名称` / `技术参数` / `数量` / `单位`

默认不要新增 `工程名称`、`所属系统`、`所属分类` 等列。

## 动态分类方法论（核心）

分类的核心目标：

> 根据当前工程类型、当前工程全部物料以及已有同类型工程案例，动态生成符合工程人员实际整理习惯的分类目录。

**分类不是固定白名单。** 不得预先规定某设备一定属于某固定类、某工程一定必须存在某几个分类。分类必须服从当前工程实际结构。

遵循：**案例定形态，方法论定调整，护栏定边界，当前工程定最终结果。**

### 1. 案例定形态

`references/category_cases.md` 是工程分类案例库。分类前**必须实际读取**，用于学习：

- 分类颗粒度
- 合并方式
- 拆分方式
- 分类命名风格
- 工程功能关系

案例不是固定分类表。当前工程与某案例高度相似时，优先参考该案例形态，再按实际物料调整。

### 2. 当前工程决定最终分类

即使同一种工程，不同项目也允许产生不同目录。例如两个智能化项目：

- 工程 A 网络设备较少 → 只有「网络通信类」
- 工程 B 网络设备很多 → 拆成「网络交换设备」「光纤网线类」「光模块及光纤配件」

同类工程分类形态应相似，但不是完全一样。

### 3. 分类优先级

分类判断优先级：

> **工程功能 > 所属子系统 > 设备/材料属性 > 安装与采购关联性**

禁止只根据关键词分类。不能因为名称里出现"交换"就直接进入网络设备，不能因为出现"柜"字就直接进入机柜类。必须结合：

- 设备是什么
- 在当前工程里做什么
- 属于什么系统
- 与哪些设备共同工作
- 当前项目中这一专业的规模

### 4. 必须先整体分析，再逐项分类

禁止"读取一条 → 立即定分类 → 读取下一条"。正确流程：

```text
读取全部工程物料
        ↓
识别工程类型和专业构成
        ↓
判断是否为单专业或复合工程
        ↓
读取一个或多个最相似案例
        ↓
形成候选分类骨架
        ↓
分析全部设备的功能/系统/属性
        ↓
建立分类映射
        ↓
动态合并、拆分、新建、删除类别
        ↓
类名自洽性校验
        ↓
粒度校验
        ↓
唯一归类校验
        ↓
未分类复核
        ↓
最终输出
```

### 5. 必须支持复合工程

一个工程可能同时存在供配电、智能化、暖通、给排水、机械设备、安防、网络等专业。不得强行把整个工程识别成单一类型。复合工程处理：

1. 识别其中包含的主要专业；
2. 可同时读取多个相关案例；
3. 分别学习各专业分类形态；
4. 最后统一形成当前工程目录；
5. 避免不同专业之间重复分类或互相错误吸收。

### 6. 分类映射不能只使用设备名称

不要简单建立「设备名称 → 分类」。同名设备可能出现在不同系统中（例如"控制箱"可能属于除尘系统、暖通系统、给排水系统或配电系统）。分类判断至少结合：

```text
来源章节/所属系统 + 设备名称 + 规格参数 + 单位 + 上下文 → 最终分类
```

设备名称只是重要依据之一。

### 7. 动态粒度规则

分类不能过细，也不能过粗。以下数字只用于**触发重新检查**，不能直接决定分类：

- 分类总数通常 4～15 个左右，但不是硬限制；
- 某分类少于 3 个独立设备/采购项时，检查是否过细；
- 同类型未形成专门分类的设备达到约 5 项时，检查是否应该独立；
- 某分类项目很多且内部专业差异明显时，检查是否需要拆分；
- 出现大量只有 1 项的小分类时，检查是否需要合并。

**数字只触发复核，不直接决定分类。** 例如有 8 种摄像机支架，并不意味着必须新建"摄像机支架类"；如果它们明显是监控系统附件，可以继续跟随安防主体。

### 8. 附件跟随主体

系统专用附件原则上跟随主体：

- 摄像机专用支架 → 安防/视频监控
- PLC 扩展模块 → PLC/自动化
- 交换机专用电源 → 网络交换设备
- 大屏发送卡 → 显示大屏

通用辅材（扎带、螺丝、胶带、通用固定件、通用安装附件）无法确定专属系统时，可进入「安装辅材杂项」。

### 9. 类名必须自洽（硬性校验）

对最终每一个分类，必须检查：

> 分类名称能不能合理解释该类中的全部主要设备？

不是检查"这个设备历史上是不是固定属于这个类"，而是检查"当前这个类名能不能解释当前这些设备"。

例如分类名「机柜箱体」，则网络机柜、壁挂机柜、控制箱通常自洽；如果里面出现风机、路灯、高压变压器，则明显需要重新检查。不得通过示例重新偷偷建立固定白名单。

### 10. 未分类项目规则

「未分类项目」只能作为临时兜底。只有满足以下情况才能进入：

- 信息不足
- 上下文不足
- 无法判断实际用途
- 无法确认所属系统
- 现有资料不足以可靠分类

不得因为"不好分类"就大量扔入未分类。如果未分类中逐渐积累多个明显同类型设备，应重新评估是否实际上已经形成一个新的分类。进入未分类的条目必须可以说明原因。

### 11. 唯一归类原则

每一个有效设备/采购项**必须且只能进入一个最终分类**。不得：

- 同一设备同时进入两个分类
- 漏掉设备
- 为了保持模板结构而复制设备

最终必须检查：

```text
有效设备行 = 已分类设备行 + 未分类设备行
```

并确保每条有效设备只出现一次。

### 12. 写文件前先完成内部预分类

不要一边读清单一边创建目录。必须先完成：完整清单读取 → 非设备行过滤 → 工程类型分析 → 案例选择 → 候选分类生成 → 全部设备分类映射 → 合并/拆分调整 → 未分类检查 → 类名自洽检查。确认逻辑稳定以后，再正式创建分类文件夹、写 Excel、写产品说明 Word，防止前面已生成大量文件、后面发现分类结构错误又整体返工。

## 模板与案例读取

当用户说"按模板""按横沟模板""按照上述文件夹一样"或提供 `采购清单及产品说明` 文件夹时，必须先读取模板文件夹，不得凭默认分类直接生成结果。

模板文件夹由用户给出路径；用户未给出时主动询问，不得猜测或使用写死路径。若技能包内已有用户放置的模板文件夹（如 `template/`），可优先使用并在汇报中注明。

模板和案例是**候选参考，不是固定清单**；实际分类必须根据当前设备清单内容按「动态分类方法论」确定。

按横沟模板或默认模板整理时，必须读取 `references/henggou_template_pattern.md`（格式规律）和 `references/category_cases.md`（分类案例），并按其中的 Excel 行结构、产品说明规律和分类方法输出。

读取模板时至少确认：

- 根目录下的分类子文件夹名称，例如 `机柜箱体配电`、`线缆管材类`、`网络交换设备`、`工控服务器 UPS`、`安装辅材杂项` 等，用来学习分类颗粒度和命名方式（**是学习参考，不是必须照抄的分类清单**）。
- 每个分类子文件夹内通常包含一个分类 Excel 和一个产品说明 Word；输出必须复刻这种结构。
- Excel 第一行是分类标题，第二行是表头；数据从第三行开始。
- 常见表头为 `序号 / 设备名称 / 技术参数 / 数量 / 单位`，个别分类可用 `器材名称`、`软件名称`、`技术特征`，应沿用最接近的模板口径。
- 部分模板允许后续行 `序号` 和 `设备名称` 留空，只填写新的 `技术参数 / 数量 / 单位`，表示同一设备的不同规格；整理输出时按「断行和续写行」的自动补全规则补全名称并标注来源，不能静默丢弃或强行合并。
- 根目录可能存在 `未分类项目.xlsx`；无法可靠归类的设备先放入该文件，不要硬塞到不匹配的分类。
- 产品说明 Word 的文件名、一级标题、二级标题编号、段落密度和工程化语气必须参考同类模板 Word。

分类输出硬性要求：

- 不能只生成一个 `供电系统采购清单.xlsx` 或一个总说明 Word 来替代模板文件夹。
- 分类子文件夹的数量和名称**完全由当前工程实际物料和「动态分类方法论」决定**；模板不决定分类，只参考其输出结构（文件夹 + Excel + Word 的组织方式）。
- 每个有数据的分类子文件夹内放置对应分类 Excel 和产品说明 Word。
- 设备归类按「动态分类方法论」执行：先整体分析、按工程功能/子系统/属性判断，结合来源章节和上下文；不能确定归类时进入 `未分类项目.xlsx`，不要硬塞到近似但不准确的分类。
- 分类 Excel 中数量合并仍遵守“同名 + 同单位 + 同规格才合并”的规则；参数不同必须保留为下一行或独立行。
- 每个分类必须通过「类名自洽」校验（见「动态分类方法论」第 9 条）：类名能解释类内全部主要设备；类内出现类名无法解释的设备时必须调整分类或类名，严禁为了塞入模板分类而扭曲归类。

## 文件读取与转换

处理 Word、PDF、Excel、WPS 文件时始终只读原文，需要解析或检索时先转临时副本：

- Word/WPS（.docx）：用 `python-docx` 读取段落与表格文本；`.doc`、`.wps`、`.docm`、宏启用 Word 兼容文件，或 `python-docx` 无法直接读取的 Word/WPS 文件，统一先用 LibreOffice（`soffice --headless --convert-to docx` 或 `--convert-to txt:Text`）转为临时 `.docx` 或 `.txt`。
- PDF：优先提取文本层（`pdfplumber` 或 `PyMuPDF`）；扫描件无文本层时用 OCR，并标注识别结果需复核。
- Excel（.xlsx/.xls）：用 `openpyxl` 或 `pandas` 读取；`.xls` 旧格式先转 `.xlsx` 或用 `xlrd` 读取。
- 转换产物一律写入临时目录，严禁覆盖或改动原始文件。

## 原始资料提取规则

从原始项目资料提取时：

1. 先确认用户要求的章节、起止位置、截图标记或表格范围。
2. 先按原文结构提取表格行，再做整理和合并。
3. 原始清单中尽量保持原文顺序。
4. 原文规格为空就保持为空，不自行补写。
5. `工程名称`、`系统名称`、`（一）原煤仓配电室` 等标题行如果属于原文表格结构，可以作为标题行保留。
6. 标题行不是设备材料行，不能参与采购数量统计。
7. 安装工序行（如 `电缆敷设`、`立杆安装`、`照明灯具安装`、`水泵安装`、`水泵配管`，名称以“安装/敷设”结尾的工序项）不是设备材料，一律剔除，不进原始清单和采购清单。**但安装工序不能只依靠字符串后缀机械判断**：不能简单规定所有包含"安装"的名称都删除，需结合单位、数量、参数、上下文、是否明显描述施工动作综合判断（例如"安装辅材""安装套件"是设备材料，不是工序行）。

## 标题行处理

常见标题行包括：

- `工程名称：1. 配电系统`
- `系统名称：...`
- `（一）原煤仓配电室`
- `一、供配电设备`

处理规则：

- 可作为独立标题行保留在原始清单中。
- 不作为设备或材料。
- 不计入采购总数。
- 默认不因此增加 `工程名称` 或 `所属分类` 列。

## 采购合并规则

生成 `采购清单及产品说明/` 时，按采购口径汇总：

先做规格归一化再判断是否相同：忽略前后/连续空格、全角半角标点差异；表格换行断开的参数先拼回（如 `...0.4kV` + `100kVA)` 拼成一行）再比对。归一化后相同视为同一规格，可合并数量并备注「已按归一化合并，需复核」；归一化后仍不同则分行保留。

- 设备名称相同 + 单位相同 + 规格参数相同（归一化后）：合并数量。
- 设备名称相同 + 单位相同 + 规格参数都为空：合并数量。
- 设备名称相同 + 单位相同 + 规格参数不同（归一化后仍不同）：严禁合并，必须分行保留。
- 设备名称相同 + 单位相同 + 一个规格为空、一个规格非空：严禁合并，必须分行保留。
- 设备名称相同 + 单位不同：严禁合并，必须分行保留。

数量合并必须做算术相加。无法可靠解析的数量不要强行计算，应保留原值并标注待确认。

典型示例：

- `防爆二联按钮盒 / 空 / 套 / 24` 与 `防爆二联按钮盒 / 空 / 套 / 13` 可合并为 `防爆二联按钮盒 / 空 / 套 / 37`。
- `检修电源箱 / WRBK043 / 台 / 4` 与 `检修电源箱 / NMXZS3029A / 台 / 2` 参数不同，必须保留两行。
- `光纤 / 8芯单模光缆 / m / 100` 后续空序号行 `单模8芯铠装 / m / 200` 可将设备名称补为 `光纤`，但作为独立规格行保留，最终采购序号重新连续编号。

## 规格参数完整性

规格参数不同代表不同采购项。不能为了汇总把不同规格写成一行，例如 `规格A；规格B`，除非原文自身就是这样表达。

示例：

- `检修电源箱 / WRBK043 / 台 / 4`
- `检修电源箱 / NMXZS3029A / 台 / 2`

这两条必须保留为两行，不能合并。

## 断行和续写行

Word/PDF 表格经常把一条设备拆成多行，这类情况风险高，必须谨慎处理。

### 只有名称、没有规格/单位/数量的行

不能默认当作新设备，也不能默认拼到上一条。

- 如果明显是上一条设备的配置说明或参数补充，可作为续写候选处理，并标注需复核。
- 如果看起来像独立设备名称，或无法判断，保留为 `待人工确认`。
- 严禁自动拼到上一条设备名称，避免把两个设备合成一个。

### 只有规格/参数、没有名称/单位/数量的行

通常可能是上一条规格参数的续写，但仍需判断。

- 如果上下文连续且语义明显，可拼接到上一条 `产品目录中的型号及规格`，并在备注中写明 `由续写行合并，需复核`。
- 如果不确定，不要拼接，放入待确认内容。

### 序号和名称留空、仅有参数/数量/单位的行（自动补全）

模板中常见：同一设备有多个规格时，只在首行写序号和设备名称，后续规格行序号、设备名称留空，仅填技术参数、数量、单位（如「光纤」下「8芯单模光缆」与「单模8芯铠装」两行）。

处理规则（自动补全，从严）：

1. 同时满足以下全部条件才自动补全：
   - 序号、设备名称均为空；
   - 技术参数非空；
   - 上一行是完整设备行（有设备名称）；
   - 该行技术参数与上一行设备属于同一设备的不同规格（按参数语义判断）。
2. 补全方式：
   - 设备名称补为上一行的设备名称；
   - 来源序号可记录为上一行序号，并在备注标注 `续写行补全，需复核`；
   - 最终采购清单序号必须重新连续编号，不能因为续写行补全而出现重复序号；
   - 每行规格仍是独立采购项，严禁与上一行合并数量。
3. 以下情况不补全，保留为 `待人工确认`：
   - 上一行无名称或上下文断裂；
   - 该行参数与上一行明显不是同类设备（如光纤下出现配电柜）；
   - 参数、数量、单位均空。
4. 补全后必须保留 `由空序号续写行补全` 的来源标记，确保可追溯、可复核，不静默改写原文。

常见续写判断线索：

- 包含 `含`、`配`、`带`、`包括`、`详见`、`满足`
- 包含参数词，如 `电压`、`功率`、`容量`、`尺寸`、`材质`、`防爆`、`IP`、`kV`、`kW`、`A`、`mm`
- 上一行规格看起来没有写完，例如以逗号、顿号、括号未闭合结尾

## 未明确确认部件规格参数文件

规格参数仍为空的设备（简称「空白规格」）需单独整理，生成：

`未明确确认部件规格参数.xlsx`

默认列名固定为：

`所属分类` / `设备名称` / `技术参数/规格` / `数量` / `单位` / `可补充候选参数` / `核查备注`

默认不加入源文件名、完整路径、原始行号等列。用户后续明确需要时再添加。

## 候选参数检索

当用户要求在项目文件夹中查找可补充参数时：

1. 只检索用户指定的项目文件夹，不混入无关项目。
2. 必要时按「文件读取与转换」把 Word、WPS、PDF、Excel 转成临时可检索文本。
3. 优先按设备名称精确匹配。
4. 只有同名记录中明确存在非空规格/参数时，才作为候选参数。
5. 价格行、单价、合价等不能当作规格参数。
6. 一般性说明文字不能直接当作参数，除非它明确描述该设备的技术参数。
7. 不直接覆盖主采购清单中的空规格。
8. 只写入 `可补充候选参数`，并在 `核查备注` 中说明不确定性。

如果候选参数来自其他专业、其他分册、其他系统，或只是同名设备，应明确备注：不能直接无条件回填，需要人工确认。

## 产品说明文件夹

按既有 `采购清单及产品说明` 模板规律整理时：

- 先读取模板文件夹，理解其输出结构和产品说明格式（文件夹 + Excel + Word 的组织方式）。
- 分类子文件夹按「动态分类方法论」由当前工程实际物料决定，模板分类名称不决定分类；分类数量、分类名称根据当前项目灵活确定。
- 每个分类通常包含一个采购清单 Excel 和一个产品说明 Word。
- 文件命名、表头、说明语气尽量沿用模板。
- 对空白规格设备，不编造具体型号或参数。
- 产品说明可以描述设备类别、用途和采购关注点，但不能虚构原文没有的型号规格。

## 产品说明 Word 写作要求

生成或改写各分类的 `产品说明.docx` 时，必须严格参照用户提供的模板 Word，而不是只生成简短介绍。

生成产品说明前，应优先检索同项目说明书、设计说明、概算说明、系统章节等资料。能检索到相关依据时，产品说明必须结合这些依据改写；不能只根据 Excel 清单泛写。

如果用户没有提供更具体的产品说明模板，先读取 `references/product_description_template.md`，按其中默认章节骨架组织内容。

先对模板 Word 做结构拆解，至少确认：

- 一级标题格式，例如 `光纤网线类产品说明`。
- 二级标题编号方式，例如 `一、整体网络光缆敷设通用要求`、`二、工业环网光纤配置要求`。
- 正文段落密度、每节说明长度、是否使用表格。
- 语言风格：正式、工程化、采购说明口径。
- 内容来源：是否体现设计依据、施工标准、系统架构、安装场景、设备用途、配套关系、传输/供电/控制逻辑等。

写作时必须满足：

- 章节数量、标题层级、段落组织应尽量贴近模板；模板是多节说明时，新文件也不能只写一两段。
- 内容丰富度不得明显低于模板；每个分类至少说明通用要求、核心设备/材料用途、系统或施工逻辑、配套设备关系、采购关注点。
- 权威度要来自项目资料、设计说明、设备清单、施工或行业通用规范口径；不能写成泛泛营销文案。
- 可以使用 `依据设计文件、施工标准、系统架构要求` 等工程化表述，但不得伪造具体规范编号、认证、品牌、型号或性能指标。
- 原文已有明确型号、规格、参数时应准确引用；原文没有的参数不能编造。
- 对空白规格设备，只能写类别用途、适用场景、采购核验要点，不能写确定型号。
- 生成后要检查标题编号连续、段落完整、表达专业、无明显模板遗漏。

例如参考模板 `光纤网线类产品说明.docx` 的规律（仅作示例，实际以用户提供的模板为准）：一级标题为分类名称产品说明，正文按 `一、二、三...` 分为多节，内容围绕通用要求、设备配置、系统或施工逻辑、配套设备、对接关系和施工辅材展开。处理其他分类时应达到类似的结构完整度和工程说明深度，而不是照搬光纤网线的具体章节。

## 防错校验（工程文件整理强制流程）

整理工程文件（概算书、设备清册、采购清单）时，按以下防错流程执行，任一环节不满足即视为未完成，不得交付：

### 1. 数量守恒校验（必做）

整理完成后对账，分三层，口径必须清晰：

- **提取层守恒**：`源表格行 = 设备行 + 剔除的非设备行 + 空行/无效行`（剔除层：安装工序行、价格/汇总行、表头、页眉页脚、章节标题，剔除数量须留痕，能逐类说清）。
- **有效设备守恒**：`有效设备行 = 已分类设备行 + 未分类设备行`，且每条有效设备只出现一次（唯一归类）。
- **合并层守恒**：合并后总项数只能 ≤ 合并前有效设备行数，差额必须是同名同规格归并，且能解释。

任何一层对不上，必须排查到解释清楚为止，不得交付。

### 2. 类名自洽与边界校验（必做）

对**当前工程最终生成的每一个分类**做「类名能解释该类全部主要设备」校验（见「动态分类方法论」第 9 条）。类内出现无法用类名解释的设备（如「工控服务器 UPS」里出现路灯、风机、空调，「机柜箱体配电」里出现高压变压器）即判定分类失败，必须修正。

命名明确的关键类可用边界定义做程序化抽查（如 `工控服务器 UPS`、`网络交换设备`、`光纤网线类`、`机柜箱体配电` 出现在当前工程中时），但**不得反向推导**——不能因为脚本里有这些类名就要求当前工程必须存在这些分类。分类是否存在、叫什么，由当前工程物料决定；边界校验只在分类真实存在时生效。

### 3. 提取侧统一过滤（必做）

提取时一次性过滤 5 类非设备行，并计数留痕：

1. 安装工序行（以“安装/敷设”结尾）：`电缆敷设`、`立杆安装`、`照明灯具安装`、`水泵安装`、`水泵配管`。**注意不能只靠后缀机械判断**（“安装辅材”“安装套件”是材料不是工序），需结合单位、数量、参数、上下文综合判断（见「原始资料提取规则」第 7 条）。
2. 价格/汇总行：`计`、`合计`、`增值税`、`运杂费`、`备品备件费`、`安装费`、`税金`。
3. 表头行：每页重复的多级表头。
4. 页眉页脚：工程名+文档名、页码。
5. 章节标题：`十五、供电系统`、`1.既有电气改造`。

### 4. 分类映射先行（推荐，结合系统上下文）

分类前先建立映射：源文件所有设备**结合来源章节/所属系统 + 设备名称 + 规格参数 + 单位**逐一指定去向（禁止只按设备名称映射，同名设备可能属于不同系统）。匹配不到的必须显式处理（新建分类或进 `未分类项目.xlsx`），不得静默硬塞。映射是过程产物，须能追溯到每个设备的去向，并保证唯一归类（同一设备不重复进入两个分类）。

### 5. 工程特性兜底（推荐）

- 单位统一小写后再比对（m/M、kW/KW、kVA/KVA）。
- 规格全角/半角符号统一后再比对（已在「采购合并规则」归一化）。
- 子章节编号重复时用「父章节+章节名」追溯，不依赖序号。
- 设备近名（如 防爆检修电源箱 vs 防爆检修插座箱）按完整名称+规格判断，不按“包含”合并。
- 扫描 PDF 无文本层时必须 OCR，并标注“识别需复核”。

## 核查清单

完成前必须检查：

- 输出文件是否存在且可打开/读取。
- 原始设备材料清单提取了多少行。
- 采购清单合并后多少项。
- 空白规格多少项。
- 如做候选参数检索，找到多少项候选参数。
- 标题行是否被错误计入设备数量。
- 不同规格是否被错误合并。
- 抽查代表性行是否与原始资料一致。
- 分类前是否已读取 `references/category_cases.md`；复合工程是否读取了多个相关案例。
- 每个分类类名是否自洽（能解释类内全部主要设备）。
- 是否满足唯一归类：每条有效设备只进入一个分类，无重复、无遗漏（`有效设备行 = 已分类设备行 + 未分类设备行`）。
- 抽查命名明确的关键分类（如 `工控服务器 UPS`、`机柜箱体配电`、`网络交换设备`，仅当该类在当前工程中存在时）是否混入无关设备（如路灯、风机、空调、电缆等）。
- 未分类项目是否都是真正信息不足、无法判断的设备，且能说明原因；是否积累出可独立成类的同类设备而未识别。
- 检查 `安装辅材杂项` 等兜底分类是否误收安装工序行（如 `电缆敷设`、`立杆安装`、`照明灯具安装`、`水泵安装`）。
- 产品说明 Word 是否严格贴合模板格式、内容丰富度和工程化权威表达。
- 产品说明 Word 修改后必须能用 `python-docx` 读取。
- 产品说明 Word 修改后若环境存在 `render_docx.py`（外部渲染脚本，不在本技能 scripts/ 内），用它渲染为 PNG 检查每个文件至少 1 页、无空白页、无明显重叠、无异常连续标点；脚本缺失时跳过渲染检查，保留 `python-docx` 可读检查。
- 若已生成采购文件夹，优先运行 `scripts/check_procurement_consistency.py` 做一致性检查。
- 原始文件是否保持未修改。

## 最终汇报格式

最终回复使用简洁中文格式：

- 输出文件：列出生成的文件和文件夹。
- 原始清单：说明提取行数。
- 采购清单：说明合并后条数和合并规则。
- 空白规格：说明空白规格条数。
- 候选参数：如检索过，说明找到多少条候选。
- 核查结果：说明已核查哪些关键点。
- 需人工确认：概括仍有风险或需要专业确认的内容。
- 原始文件：说明只读取，未修改。

汇报要短、清楚、可直接交给负责人看。
